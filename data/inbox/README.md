# Inbox

Drop Yahoo screenshots / pasted text here via Transmit. Claude reads,
normalizes into the dashboard data, and archives processed files to
data/raw/ (gitignored). Any filename is fine; week prefixes like wk1-
help but are optional.
